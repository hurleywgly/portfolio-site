import { PagePlaceholder } from "@/components/page-placeholder"

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      slug="projects"
      title="Systems in motion."
      description="Selected products, prototypes, and live systems will be assembled here in the next page-build stage."
      grid="02"
    />
  )
}
