import { PagePlaceholder } from "@/components/page-placeholder"

export default function MethodologyPage() {
  return (
    <PagePlaceholder
      slug="methodology"
      title="Models change, but the system holds."
      description="The frameworks, lenses, and operating protocols behind the work will be rebuilt here from the locked design."
      grid="12"
    />
  )
}
