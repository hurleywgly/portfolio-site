import { PagePlaceholder } from "@/components/page-placeholder"

export default function AboutPage() {
  return (
    <PagePlaceholder
      slug="about"
      title="Always tinkering."
      description="A concise account of the builder, the career arc, and the systems behind the work will land here next."
      grid="01"
    />
  )
}
